export interface User {
    inr: number, paisa: number, streetaddress: string, city: string,
        state: string, country: string, pincode: number, phonenumber: number,
        email: string, firstname: string, lastname: string, age: number,
        trainerpreference: string, physiotherapist: string, packages: string,
        id: number
  }